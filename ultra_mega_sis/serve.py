def serve(host, port, debug=True):
    """
    Serve the app using Flask
    
    Args:
        host (str): Host to serve the app on
        port (int): Port to serve the app on
        debug (bool): Whether to run the app in debug mode
    """
